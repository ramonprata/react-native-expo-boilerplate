import { BoxContent, Loader } from "@shared/components";

const LoadingEpisodes = () => {
  return (
    <BoxContent center>
      <Loader />
    </BoxContent>
  );
};

export default LoadingEpisodes;
